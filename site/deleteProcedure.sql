CREATE OR REPLACE PROCEDURE DATA_DELETE
(
	PID TESTSET.ID%TYPE
)
AS

BEGIN
	 DELETE FROM TESTSET WHERE ID = PID;
	 COMMIT;

END;
/

DECLARE
	TID TESTSET.ID%TYPE;
	NEGATIVE_VALUE EXCEPTION;

BEGIN

	TID := &ID;
	
	dbms_output.put_line(TID);


	IF TID<0 THEN
		RAISE NEGATIVE_VALUE;
	END IF;

	DATA_DELETE(TID);


	COMMIT;

	EXCEPTION
		WHEN NEGATIVE_VALUE THEN
			DBMS_OUTPUT.PUT_LINE('NEGATIVE VALUE');

END;
/
